<odoo>

    <record id="inherit_stock_location" model="ir.ui.view">
        <field name="name">stock.location.inherit.form</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <field name="return_location" position="after">
                <field name="is_transit"/>
                <field name="is_foc"/>
            </field>
            <xpath expr="//group/group[@name='additional_info']" position="before">
                <label for="address"/>
                <field name="address" placeholder="e.g. Address"/>
                <label for="phone"/>
                <field name="phone" placeholder="e.g. Phone"/>
            </xpath>
        </field>

    </record>

</odoo>